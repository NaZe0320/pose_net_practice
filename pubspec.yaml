name: pose_net_practice
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.0-42.2.beta <4.0.0'

dependencies:
  camera: ^0.10.5+9
  flutter:
    sdk: flutter
  google_mlkit_commons: ^0.6.1
  google_mlkit_pose_detection: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
